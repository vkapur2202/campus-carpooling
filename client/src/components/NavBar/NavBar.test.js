import * as React from "react";
import * as ReactDOM from "react-dom";
//import NavBar from './NavBar';

it("renders the correct content", () => {
    const root = document.createElement("div");
    //ReactDOM.render(<NavBar></NavBar>, root);
});