import * as React from "react";
import * as ReactDOM from "react-dom";
//import Profile from './Profile';

it("renders the correct content", () => {
    const root = document.createElement("div");
    //ReactDOM.render(<Profile></Profile>, root);
});