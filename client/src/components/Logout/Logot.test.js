import * as React from "react";
import * as ReactDOM from "react-dom";
//import Logout from './Logout';

it("renders the correct content", () => {
    const root = document.createElement("div");
    //ReactDOM.render(<Logout></Logout>, root);
});